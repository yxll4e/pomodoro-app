name: pomodoro_video
description: تطبيق Pomodoro احترافي مع خلفية فيديو (اختيار ملف أو رابط)، إشعارات، حفظ إعدادات، ودعم اختصارات.
publish_to: "none"
version: 1.0.0+1
environment:
  sdk: ">=2.18.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  video_player: ^2.5.1
  file_picker: ^5.2.3
  shared_preferences: ^2.1.1
  flutter_local_notifications: ^12.0.4
  wakelock: ^0.6.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/
